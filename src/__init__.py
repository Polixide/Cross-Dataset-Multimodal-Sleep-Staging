"""Source package for the cross-dataset sleep-staging benchmark.

Modules are imported as ``from src.<module> import ...`` (see conftest.py and the
path shim in scripts/). Keep reusable logic here; keep scripts/ as thin entry
points and notebooks/ for exploration only.
"""
